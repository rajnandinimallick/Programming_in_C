#include <stdio.h>

int main(){
    int Int, /*char,*/ /*6_05,*/ Calloc, Xx, alpha_beta_routine, floating, _1312, z, ReInitialize, _ /*,A$*/;

    //commented out variable are invalid as 1st one indicates preerved words, 2nd one starts with a number and 3rd one has a non-alphabetic character that is not underscore(_)

    return 0;
}
